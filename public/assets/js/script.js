const updateRequest = document.querySelector('#updateRequest');

const showUpdateRequest = (id) =>{
  updateRequest.style.display='flex';
}
const hideUpdateRequest = () =>{
  updateRequest.style.display='none';
}